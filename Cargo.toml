[package]
name = "openairplay"
version = "0.1.0"
edition = "2021"
description = "AirPlay 1 (RAOP) receiver for Linux/ALSA"
license = "MIT"

[dependencies]
base64 = "0.22"
env_logger = "0.11"
log = "0.4"
rsa = "0.9"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "signal", "sync", "time"] }
